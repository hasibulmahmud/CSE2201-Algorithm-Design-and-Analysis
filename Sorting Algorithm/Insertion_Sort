#include<bits/stdc++.h>
using namespace std;
int main(){
    int arr[] = {6,5,1,3,4,2};
    for(int i=1;i<6;i++){
        int j=i-1;
        int key = arr[i];
        while(j>=0 && arr[j]>key){
            arr[j+1] =arr[j];
            j--;
        }
        arr[j+1] = key;

    }
    for(int i=0;i<6;i++){
        cout << arr[i] <<" ";
    }
    cout << endl;
}
